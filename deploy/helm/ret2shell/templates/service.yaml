apiVersion: v1
kind: Service
metadata:
  name: {{ include "ret2shell.fullname" . }}
  labels:
    {{- include "ret2shell.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ret2shell.selectorLabels" . | nindent 4 }}
