platform:
  image:
    tag: "3.10.9"
  config:
    auth:
      signingKey: replace-with-a-long-random-string
  blocked:
    content: ""

postgresql:
  auth:
    database: ret2shell
    username: ret2shell
    password: replace-with-postgresql-password

valkey:
  auth:
    password: replace-with-valkey-password

nats:
  auth:
    token: replace-with-nats-token
